import { GlassBackground } from '@/components/ui/GlassBackground';

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return <GlassBackground>{children}</GlassBackground>;
}
